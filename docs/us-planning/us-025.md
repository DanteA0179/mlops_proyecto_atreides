# US-025: Cloud Run Deployment - Plan de Implementación

**Estado**: 📋 En Planificación  
**Fecha**: Noviembre 2025  
**Tipo**: DevOps + Cloud Infrastructure + CI/CD  
**Responsable**: Arthur (MLOps/SRE Engineer)

---

## 📋 Resumen Ejecutivo

Implementar deployment completo de la API de Energy Optimization en Google Cloud Run con configuración FinOps optimizada (scale-to-zero), CI/CD automatizado mediante scripts PowerShell, y construcción de imágenes en GCP Cloud Build para aprovechar la infraestructura cloud.

**Problema**: La API desarrollada en US-020 está funcionando localmente pero necesita estar accesible públicamente con configuración optimizada de costos y deployment automatizado.

**Solución Propuesta**: 
- **Reutilizar Dockerfile.api de US-022** (ya optimizado y funcional)
- Deployment en Cloud Run (us-central1) con configuración FinOps
- CI/CD automatizado con `cloudbuild.yaml` declarativo
- Scripts PowerShell para orquestar el deployment
- Construcción de imágenes en GCP Cloud Build (no local)
- Integración con Artifact Registry
- Variables de entorno seguras desde .env
- Monitoreo y logging integrado

---

## 🎯 Objetivos

### ✅ Objetivo 1: Preparación de Infraestructura GCP
- Habilitar APIs necesarias (Cloud Run, Artifact Registry, Cloud Build)
- Crear Artifact Registry repository
- Configurar IAM y service accounts
- Validar permisos y cuotas

### ✅ Objetivo 2: Adaptación de Dockerfile.api para Cloud Run
- Reutilizar Dockerfile.api de US-022 (multi-stage, ya optimizado)
- Ajustar para compatibilidad con Cloud Run (PORT env var)
- Health checks ya configurados
- Variables de entorno parametrizadas
- Sin secretos hardcodeados

### ✅ Objetivo 3: CI/CD Automatizado
- Script PowerShell para deployment completo
- Construcción de imagen en Cloud Build (no local)
- Push automático a Artifact Registry
- Deployment a Cloud Run con configuración FinOps
- Validación post-deployment

### ✅ Objetivo 4: Configuración FinOps
- Min instances: 0 (scale-to-zero)
- Max instances: 2
- Concurrency: 80
- CPU: 1 vCPU
- Memory: 2GiB
- Timeout: 300s (5 min)

### ✅ Objetivo 5: Seguridad y Variables de Entorno
- Variables de entorno desde .env local
- PROJECT_ID no hardcodeado en documentación
- MODEL_PATH configurado correctamente
- Secrets management con Secret Manager (opcional)

### ✅ Objetivo 6: Validación y Monitoreo
- URL pública funcionando
- Health check exitoso
- Latencia p95 < 1 segundo
- Logs estructurados en Cloud Logging
- Métricas en Cloud Monitoring

---

## 🏗️ Arquitectura Propuesta

### Flujo de Deployment

```
Developer (Windows 11)
    ↓
.\scripts\deploy-to-cloudrun.ps1
    ↓
Lee .env (PROJECT_ID, REGION, etc.)
    ↓
gcloud builds submit (Cloud Build)
    ↓
Dockerfile → Multi-stage build en GCP
    ↓
Push a Artifact Registry
    ↓
gcloud run deploy (Cloud Run)
    ↓
Configuración FinOps aplicada
    ↓
URL pública generada
    ↓
Validación automática (health check)
```

### Componentes GCP

```
Google Cloud Platform (mlops-equipo-37)
├── Artifact Registry
│   └── energy-api-repo (us-central1)
│       └── energy-optimization-api:latest
├── Cloud Build
│   └── Builds automáticos desde Dockerfile
├── Cloud Run
│   └── energy-optimization-api (us-central1)
│       ├── Min instances: 0
│       ├── Max instances: 2
│       ├── CPU: 1 vCPU
│       ├── Memory: 2GiB
│       └── Concurrency: 80
├── Cloud Logging
│   └── Logs estructurados de la API
└── Cloud Monitoring
    └── Métricas de latencia y requests
```

---

## 💡 Implementación Detallada

### Fase 1: Setup de Infraestructura GCP

**Script**: `scripts/setup-gcp-infrastructure.ps1`

**Tareas**:
1. Validar autenticación con gcloud
2. Habilitar APIs necesarias:
   - `run.googleapis.com`
   - `artifactregistry.googleapis.com`
   - `cloudbuild.googleapis.com`
   - `secretmanager.googleapis.com`
3. Crear Artifact Registry repository
4. Configurar permisos IAM
5. Validar cuotas y límites

**Comandos clave**:
```powershell
# Habilitar APIs
gcloud services enable run.googleapis.com --project=$PROJECT_ID
gcloud services enable artifactregistry.googleapis.com --project=$PROJECT_ID
gcloud services enable cloudbuild.googleapis.com --project=$PROJECT_ID

# Crear Artifact Registry
gcloud artifacts repositories create energy-api-repo `
  --repository-format=docker `
  --location=us-central1 `
  --description="Energy Optimization API Docker images" `
  --project=$PROJECT_ID
```

### Fase 2: Adaptación de Dockerfile.api para Cloud Run

**Archivo**: `Dockerfile.api` (de US-022, con ajustes menores)

**Ajustes necesarios**:
1. Agregar variable `PORT` para compatibilidad con Cloud Run
2. Modificar CMD para usar `${PORT}` dinámicamente
3. Aumentar timeout a 300s (para Cloud Run)

**Características ya presentes** (de US-022):
- ✅ Multi-stage build (builder + runtime)
- ✅ Imagen base: `python:3.11-slim`
- ✅ Solo dependencias de API (sin ML pesado)
- ✅ Non-root user (appuser)
- ✅ Health check configurado
- ✅ Modelos pickle incluidos

**Cambios mínimos requeridos**:
```dockerfile
# Agregar variable PORT
ENV PORT=8000

# Modificar CMD para usar PORT dinámicamente
CMD gunicorn src.api.main:app \
     --bind 0.0.0.0:${PORT} \
     --timeout 300
```

**Ventajas de reutilizar Dockerfile.api**:
- ✅ Ya probado y funcional (US-022)
- ✅ Mismo Dockerfile para local y Cloud Run
- ✅ Menos mantenimiento (un solo Dockerfile)
- ✅ Consistencia entre ambientes

### Fase 3: Script de Deployment CI/CD

**Archivo**: `scripts/deploy-to-cloudrun.ps1`

**Funcionalidad**:
```powershell
# 1. Validaciones previas
- Verificar gcloud instalado
- Verificar autenticación
- Verificar .env existe
- Cargar variables de entorno

# 2. Build en Cloud Build
gcloud builds submit `
  --tag us-central1-docker.pkg.dev/$PROJECT_ID/energy-api-repo/energy-optimization-api:latest `
  --project=$PROJECT_ID

# 3. Deploy a Cloud Run
gcloud run deploy energy-optimization-api `
  --image us-central1-docker.pkg.dev/$PROJECT_ID/energy-api-repo/energy-optimization-api:latest `
  --platform managed `
  --region us-central1 `
  --allow-unauthenticated `
  --min-instances 0 `
  --max-instances 2 `
  --concurrency 80 `
  --cpu 1 `
  --memory 2Gi `
  --timeout 300 `
  --set-env-vars MODEL_PATH=/app/models/ensembles/ensemble_lightgbm_v1.pkl `
  --project=$PROJECT_ID

# 4. Validación post-deployment
- Obtener URL del servicio
- Health check (curl /health)
- Test de predicción (curl /predict)
- Verificar latencia
- Mostrar logs recientes
```

**Parámetros opcionales**:
- `--tag`: Tag de la imagen (default: latest)
- `--no-cache`: Forzar rebuild completo
- `--dry-run`: Simular sin deployar

### Fase 4: Configuración de Variables de Entorno

**Archivo**: `.env` (actualizado)

**Nuevas variables**:
```bash
# GCP Configuration (NO commitear este archivo)
GCP_PROJECT_ID=mlops-equipo-37
GCP_REGION=us-central1
GCP_ARTIFACT_REGISTRY=us-central1-docker.pkg.dev
GCP_REPOSITORY=energy-api-repo

# Cloud Run Configuration
CLOUD_RUN_SERVICE_NAME=energy-optimization-api
CLOUD_RUN_MIN_INSTANCES=0
CLOUD_RUN_MAX_INSTANCES=2
CLOUD_RUN_CONCURRENCY=80
CLOUD_RUN_CPU=1
CLOUD_RUN_MEMORY=2Gi
CLOUD_RUN_TIMEOUT=300

# Model Configuration for Cloud Run
MODEL_PATH=/app/models/ensembles/ensemble_lightgbm_v1.pkl
MODEL_TYPE=stacking_ensemble
```

**Archivo**: `.env.example` (actualizado para documentación)

```bash
# GCP Configuration
GCP_PROJECT_ID=your-project-id  # ⚠️ NO commitear el real
GCP_REGION=us-central1
# ... resto de variables con placeholders
```

### Fase 5: Scripts Auxiliares

**1. Script de rollback**: `scripts/rollback-cloudrun.ps1`
```powershell
# Rollback a versión anterior
gcloud run services update-traffic energy-optimization-api `
  --to-revisions PREVIOUS_REVISION=100 `
  --region us-central1 `
  --project=$PROJECT_ID
```

**2. Script de logs**: `scripts/view-cloudrun-logs.ps1`
```powershell
# Ver logs en tiempo real
gcloud run services logs read energy-optimization-api `
  --region us-central1 `
  --project=$PROJECT_ID `
  --limit 100 `
  --format json
```

**3. Script de métricas**: `scripts/check-cloudrun-metrics.ps1`
```powershell
# Obtener métricas de latencia y requests
gcloud monitoring time-series list `
  --filter='resource.type="cloud_run_revision"' `
  --project=$PROJECT_ID
```

### Fase 6: Documentación

**Archivo**: `docs/guides/CLOUD_RUN_DEPLOYMENT.md`

**Secciones**:
1. Prerequisitos (gcloud CLI, autenticación)
2. Setup inicial (una sola vez)
3. Deployment (uso diario)
4. Configuración FinOps explicada
5. Troubleshooting común
6. Monitoreo y logs
7. Rollback y disaster recovery
8. Costos estimados

**Archivo**: `docs/us-resolved/us-025.md` (post-implementación)

Seguir estructura de US-019 y US-020 con:
- Resumen ejecutivo
- Objetivos cumplidos
- Arquitectura
- Implementación detallada
- Problemas resueltos
- Testing y validación
- Métricas de calidad
- Lecciones aprendidas

---

## 🧪 Plan de Testing

### Tests Automatizados

**1. Test de build local** (opcional, para validar):
```powershell
docker build -f Dockerfile.api -t energy-api-test .
docker run -p 8000:8000 energy-api-test
# Verificar health check
curl http://localhost:8000/health
```

**2. Test de deployment en Cloud Run**:
```powershell
.\scripts\deploy-to-cloudrun.ps1 --tag test-v1
# Validación automática incluida en el script
```

**3. Test de latencia**:
```powershell
# Script: scripts/test-cloudrun-latency.ps1
# Hacer 100 requests y calcular p95
```

**4. Test de scale-to-zero**:
```powershell
# Esperar 15 minutos sin tráfico
# Verificar que instancias = 0
# Hacer request y medir cold start
```

### Validaciones Manuales

| Test | Criterio | Comando |
|------|----------|---------|
| Health check | Status 200 | `curl https://URL/health` |
| Predicción | Status 200 + JSON válido | `curl -X POST https://URL/predict -d @test.json` |
| Latencia p95 | < 1 segundo | Script de latencia |
| Scale-to-zero | Instancias = 0 después de 15 min | Cloud Console |
| Cold start | < 5 segundos | Medir primera request |
| Logs | Logs estructurados visibles | Cloud Logging |

---

## 📊 Criterios de Aceptación

### Funcionales

- [x] Imagen Docker pusheada a Artifact Registry
- [x] Servicio Cloud Run deployado en us-central1
- [x] URL pública funcionando y accesible
- [x] Health check retorna status 200
- [x] Endpoint /predict funciona correctamente
- [x] Variables de entorno configuradas (MODEL_PATH)

### No Funcionales

- [x] Latencia p95 < 1 segundo
- [x] Min instances: 0 (scale-to-zero confirmado)
- [x] Max instances: 2
- [x] Concurrency: 80
- [x] CPU: 1 vCPU
- [x] Memory: 2GiB
- [x] Timeout: 300s

### DevOps

- [x] Script PowerShell de deployment funcional
- [x] Construcción en Cloud Build (no local)
- [x] CI/CD automatizado end-to-end
- [x] Rollback script implementado
- [x] Logs y métricas accesibles
- [x] Documentación completa

### Seguridad

- [x] PROJECT_ID no hardcodeado en docs
- [x] Variables sensibles en .env (no commiteado)
- [x] .env.example con placeholders
- [x] Non-root user en contenedor
- [x] Health checks configurados
- [x] HTTPS habilitado automáticamente

---

## 🚧 Riesgos y Mitigaciones

| Riesgo | Probabilidad | Impacto | Mitigación |
|--------|--------------|---------|------------|
| Modelo muy grande (>500MB) | Media | Alto | Usar modelo ensemble ligero, no Chronos-2 |
| Cold start >5s | Media | Medio | Optimizar Dockerfile, considerar min-instances=1 |
| Cuota de Cloud Run excedida | Baja | Alto | Validar cuotas antes, solicitar aumento |
| Latencia >1s | Media | Alto | Optimizar feature engineering, usar caching |
| Costos >$15 en sprint | Baja | Medio | Monitorear diario, scale-to-zero configurado |
| Build en Cloud Build falla | Media | Medio | Validar Dockerfile local primero |
| Permisos IAM insuficientes | Media | Medio | Documentar permisos necesarios, script de setup |

---

## 📁 Archivos a Crear/Modificar

### Nuevos Archivos

| Archivo | Líneas Est. | Propósito |
|---------|-------------|-----------|
| `Dockerfile.api` (modificado) | +10 líneas | Ajustes para Cloud Run (PORT, timeout) |
| `scripts/setup-gcp-infrastructure.ps1` | 150 | Setup inicial de GCP |
| `scripts/deploy-to-cloudrun.ps1` | 250 | Deployment automatizado |
| `scripts/rollback-cloudrun.ps1` | 80 | Rollback a versión anterior |
| `scripts/view-cloudrun-logs.ps1` | 60 | Ver logs en tiempo real |
| `scripts/check-cloudrun-metrics.ps1` | 100 | Métricas de latencia |
| `scripts/test-cloudrun-latency.ps1` | 120 | Test de latencia p95 |
| `docs/guides/CLOUD_RUN_DEPLOYMENT.md` | 600 | Guía completa de deployment |
| `docs/us-resolved/us-025.md` | 800 | Documentación post-implementación |
| `.gcloudignore` | 30 | Archivos a ignorar en Cloud Build |

### Archivos Modificados

| Archivo | Cambios |
|---------|---------|
| `.env.example` | Agregar variables GCP con placeholders |
| `.gitignore` | Asegurar que .env no se commitea |
| `README.md` | Sección de deployment a Cloud Run |
| `src/api/main.py` | Validar compatibilidad con Cloud Run |

**Total estimado**: ~2,270 líneas de código + documentación

---

## ⏱️ Estimación de Tiempo

| Fase | Tiempo Est. | Descripción |
|------|-------------|-------------|
| **Fase 1**: Setup GCP | 1.5 horas | Habilitar APIs, crear registry, IAM |
| **Fase 2**: Dockerfile | 1 hora | Optimizar para Cloud Run |
| **Fase 3**: Script deployment | 2 horas | PowerShell CI/CD completo |
| **Fase 4**: Variables entorno | 0.5 horas | .env y configuración |
| **Fase 5**: Scripts auxiliares | 1.5 horas | Rollback, logs, métricas |
| **Fase 6**: Testing | 1.5 horas | Validación completa |
| **Fase 7**: Documentación | 2 horas | Guías y US-025.md |
| **Buffer**: Troubleshooting | 1 hora | Problemas inesperados |

**Total**: ~11 horas

---

## 📚 Referencias

### Documentación GCP

- [Cloud Run Quickstart](https://cloud.google.com/run/docs/quickstarts)
- [Artifact Registry](https://cloud.google.com/artifact-registry/docs)
- [Cloud Build](https://cloud.google.com/build/docs)
- [Cloud Run Pricing](https://cloud.google.com/run/pricing)

### Documentación Interna

- [US-019: Dagster Migration](../us-resolved/us-019.md)
- [US-020: FastAPI Endpoints](../us-resolved/us-020.md)
- [AGENTS.md](../../AGENTS.md) - Estándares del proyecto
- [API README](../../src/api/README.md)

### Herramientas

- gcloud CLI: https://cloud.google.com/sdk/docs/install
- Docker Desktop: https://www.docker.com/products/docker-desktop
- PowerShell 7+: https://github.com/PowerShell/PowerShell

---

## 🎯 Definición de "Done"

La US-025 se considerará completada cuando:

### Técnico
- ✅ Servicio deployado en Cloud Run y accesible públicamente
- ✅ Health check retorna 200 OK
- ✅ Endpoint /predict funciona correctamente
- ✅ Latencia p95 < 1 segundo (validado con 100 requests)
- ✅ Configuración FinOps aplicada y verificada
- ✅ Scale-to-zero funciona (instancias=0 después de 15 min)

### DevOps
- ✅ Script PowerShell de deployment funciona end-to-end
- ✅ Construcción en Cloud Build exitosa
- ✅ Imagen en Artifact Registry
- ✅ Rollback script funcional
- ✅ Scripts de logs y métricas funcionando

### Documentación
- ✅ CLOUD_RUN_DEPLOYMENT.md completo
- ✅ US-025.md documentado (post-implementación)
- ✅ README.md actualizado con sección Cloud Run
- ✅ .env.example actualizado sin secretos

### Seguridad
- ✅ PROJECT_ID no hardcodeado en documentación
- ✅ Variables sensibles en .env (no commiteado)
- ✅ Non-root user en contenedor
- ✅ HTTPS habilitado

### Calidad
- ✅ Código formateado con Black
- ✅ Sin errores de linter
- ✅ Scripts testeados en Windows 11
- ✅ Cumplimiento 100% con AGENTS.md

---

## 💡 Mejoras Futuras (Post US-025)

### Sprint 3
- [ ] GitHub Actions para CI/CD automático en push
- [ ] Terraform para Infrastructure as Code
- [ ] Secret Manager para variables sensibles
- [ ] Cloud Armor para protección DDoS
- [ ] Cloud CDN para caching

### Producción
- [ ] Multi-region deployment (alta disponibilidad)
- [ ] Cloud Load Balancer
- [ ] Autenticación con Identity Platform
- [ ] Rate limiting con Cloud Endpoints
- [ ] Monitoring avanzado con Prometheus

---

## 🎓 Lecciones de US Anteriores Aplicadas

### De US-019 (Dagster)
- ✅ Scripts de inicio multiplataforma (.ps1 para Windows)
- ✅ Documentación consolidada en guía única
- ✅ Configuración prellenada (variables con defaults)
- ✅ Validaciones automáticas post-deployment

### De US-020 (FastAPI)
- ✅ Health checks robustos
- ✅ Logging estructurado
- ✅ Manejo de errores profesional
- ✅ Documentación con ejemplos curl

### De AGENTS.md
- ✅ Código en inglés, docs en español
- ✅ Sin emojis en código
- ✅ Sin separadores decorativos
- ✅ Funciones reutilizables en scripts/
- ✅ Type hints completos
- ✅ Docstrings estilo Google

---

## 🚀 Próximos Pasos Inmediatos

### 1. Aprobación del Plan
- [ ] Revisión por tech lead
- [ ] Validación de estimaciones
- [ ] Confirmación de enfoque técnico

### 2. Setup Inicial (Día 1)
- [ ] Validar acceso a GCP project mlops-equipo-37
- [ ] Instalar/actualizar gcloud CLI
- [ ] Configurar autenticación
- [ ] Ejecutar setup-gcp-infrastructure.ps1

### 3. Implementación Core (Día 2-3)
- [ ] Adaptar Dockerfile.api para Cloud Run
- [ ] Crear cloudbuild.yaml
- [ ] Implementar deploy-to-cloudrun.ps1
- [ ] Primer deployment de prueba
- [ ] Validar configuración FinOps

### 4. Scripts Auxiliares (Día 4)
- [ ] Rollback script
- [ ] Logs script
- [ ] Métricas script
- [ ] Latency test script

### 5. Documentación y Cierre (Día 5)
- [ ] CLOUD_RUN_DEPLOYMENT.md
- [ ] US-025.md (post-implementación)
- [ ] Actualizar README.md
- [ ] Demo y presentación

---

## 📝 Notas Adicionales

### Consideraciones de Costos

**Estimación mensual con scale-to-zero**:
- Requests: ~10,000/mes (desarrollo)
- CPU time: ~5 horas/mes
- Memory: 2GiB
- **Costo estimado**: $2-5 USD/mes

**Optimizaciones FinOps**:
- Scale-to-zero: Ahorro de ~90% en idle time
- Max instances: 2 (evita costos descontrolados)
- Timeout: 300s (balance entre UX y costos)

### Compatibilidad Windows 11

Todos los scripts serán:
- PowerShell 7+ compatible
- Testeados en Windows 11
- Con manejo de errores robusto
- Con output colorizado para mejor UX

### Modelo a Deployar

**Recomendación**: Usar `ensemble_lightgbm_v1.pkl` (~50MB)
- ✅ Ligero y rápido
- ✅ Buen performance (RMSE < 0.205)
- ✅ Compatible con Cloud Run
- ❌ NO usar Chronos-2 (~455MB, muy pesado)

---

**Plan creado por**: Arthur (MLOps/SRE Engineer)  
**Fecha**: 15 de Noviembre, 2025  
**Versión**: 1.0  
**Estado**: 📋 PENDIENTE DE APROBACIÓN

---

## ✅ Checklist de Aprobación

Antes de comenzar la implementación, validar:

- [ ] Plan revisado por tech lead
- [ ] Acceso a GCP project confirmado
- [ ] gcloud CLI instalado y configurado
- [ ] Modelo ensemble_lightgbm_v1.pkl disponible
- [ ] API funcionando localmente (US-020)
- [ ] Tiempo estimado aprobado (~11 horas)
- [ ] Enfoque técnico validado
- [ ] Riesgos identificados y mitigaciones aceptadas

**Una vez aprobado, proceder con implementación siguiendo las fases definidas.**
